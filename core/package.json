{
  "name": "@mistiboard/core",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "type": "build/index.d.ts",
  "scripts": {
    "prepack": "npm run build",
    "build": "tsc -p ./tsconfig.json",
    "build:watch": "tsc --watch -p ./tsconfig.json",
    "prettier": "prettier --write 'src/**/*.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "firebase": "^10.11.0",
    "rxjs": "^7.8.1"
  }
}
